using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class abnormalAlert : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //if !(3600 <= batt_time_left <= 10800)
        //{
        //    sound alert
        //    textComp flashing
        //    turn on alert bubble object
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
